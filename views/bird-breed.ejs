<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cow Breeds - PetCare</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://unpkg.com/aos@next/dist/aos.css" />
    <style>
      .sidebar {
        background: linear-gradient(180deg, #14b8a6, #0d9488);
        color: white;
        min-height: 100vh;
      }
      .sidebar a {
        display: flex;
        align-items: center;
        gap: 10px;
        padding: 12px 16px;
        border-radius: 8px;
        transition: background 0.3s;
      }
      .sidebar a:hover {
        background: rgba(255, 255, 255, 0.15);
      }
      .card {
        background: white;
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 6px 16px rgba(0, 0, 0, 0.08);
        transition: transform 0.3s ease;
      }
      .card:hover {
        transform: translateY(-6px);
      }
      .card img {
        width: 100%;
        height: 200px;
        object-fit: cover;
      }
      .marathi {
        color: #4b5563;
        font-style: italic;
        font-size: 0.95rem;
        margin-top: 8px;
      }
    </style>
  </head>
  <body class="flex bg-gray-100 text-gray-800">
    <!-- Sidebar -->
    <aside class="sidebar w-64 p-6 fixed top-0 left-0">
      <a
        class="text-2xl font-bold mb-8 flex items-center gap-2"
        href="/dashboard"
      >
        🐾 PetCare
      </a>
      <nav class="space-y-3">
        <a href="/animal-info/cow-care"><i class="ph ph-cow"></i> Cow Breeds</a>
        <a href="/animal-info/cow-breeds" class="text-teal-700 font-semibold"
          ><i class="bg-white ph ph-cow"></i> Buffalo Breeds</a
        >
        <a href="/animal-info/buffalo-care"
          ><i class="ph ph-cow"></i> Goat Breeds</a
        >
        <a href="/animal-info/dog-care"><i class="ph ph-dog"></i> Dog Breeds</a>
        <a href="/animal-info/cat-care"><i class="ph ph-cat"></i> Cat Breeds</a>
        <a href="/animal-info/birds-care"
          ><i class="bg-white ph ph-bird"></i> Bird Breeds</a
        >
      </nav>
    </aside>

    <!-- Main Content -->
    <main class="ml-64 w-full">
      <!-- Header -->
      <header class="bg-white shadow-md p-4 flex items-center">
        <div class="flex items-center justify-between w-full">
          <div>
            <h2 class="text-xl font-bold text-teal-700">
              Bird Breeds Information
            </h2>
          </div>
          <div>
            <a
              href="/animal-info/cow-care"
              class="bg-teal-500 text-white px-4 py-2 rounded-lg hover:bg-teal-600 transition"
            >
              Back
            </a>
          </div>
        </div>
      </header>

      <!-- Hero Section -->
      <section class="text-center py-10">
        <h1 class="text-4xl font-bold text-teal-700 mb-3" data-aos="fade-up">
          Bird Breeds
        </h1>
        <h1 class="text-4xl font-bold text-teal-700 mb-3" data-aos="fade-up">
          पक्ष्यांची जाती
        </h1>
        <p
          class="text-gray-700 max-w-2xl mx-auto"
          data-aos="fade-up"
          data-aos-delay="200"
        >
          Different Bird breeds with their special characteristics, and origin.
        </p>
        <p class="marathi">
          पक्ष्यांची विविध जाती, त्यांची वैशिष्ट्ये आणि मूळ प्रदेश.
        </p>
      </section>

      <!-- Breed Cards -->
      <section class="p-8 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        <% breeds.forEach(function(breed) { %>
        <div class="card flex flex-col">
          <div
            class="bg-white flex items-center justify-center"
            style="height: 200px"
          >
            <img
              src="<%= breed.image %>"
              alt="<%= breed.name %>"
              style="max-height: 100%; max-width: 100%; object-fit: contain"
            />
          </div>
          <div class="p-4 flex flex-col flex-grow">
            <h3 class="text-xl font-semibold text-teal-600 mb-2">
              <%= breed.name %>
            </h3>
            <p class="text-gray-700 flex-grow"><%= breed.description %></p>
            <p class="marathi"><%= breed.marathi_desc %></p>
          </div>
        </div>
        <% }) %>
      </section>

      <!-- Footer -->
      <footer class="bg-gray-800 text-white text-center py-4">
        <p>
          &copy; <%= new Date().getFullYear() %> PetCare. All Rights Reserved.
        </p>
      </footer>
    </main>

    <script src="https://unpkg.com/aos@next/dist/aos.js"></script>
    <script>
      AOS.init({ duration: 100, once: true });
    </script>
  </body>
</html>
