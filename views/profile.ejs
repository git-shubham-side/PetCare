<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= pet.Name %>'s Profile</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      body {
        font-family: "Inter", sans-serif;
        background-color: #f0f2f5;
      }
    </style>
  </head>
  <body class="p-0">
    <!-- Navbar -->
    <nav class="bg-teal-500 p-4 shadow-md">
      <div class="max-w-7xl mx-auto flex justify-between items-center">
        <div class="flex items-center">
          <span class="text-xl font-bold text-white">&#128062;PetCare</span>
        </div>
        <a href="/dashboard" class="text-white hover:underline text-m"
          >← Back to Home</a
        >
      </div>
    </nav>

    <div class="p-4 sm:p-6 md:p-8 lg:p-10">
      <div
        class="max-w-4xl mx-auto bg-white shadow-lg rounded-xl p-6 sm:p-8 mb-8"
      >
        <div
          class="flex flex-col sm:flex-row items-center sm:items-start space-y-6 sm:space-y-0 sm:space-x-8"
        >
          <!-- Pet Image -->
          <div class="w-32 h-32 sm:w-40 sm:h-40 flex-shrink-0">
            <img
              src="<%= pet.photo%>"
              alt="petImage"
              class="w-full h-full object-cover rounded-full border-4 border-blue-200 shadow-md"
              onerror="this.onerror=null;this.src='https://placehold.co/160x160/cccccc/333333?text=Pet+Image';"
            />
          </div>

          <!-- Pet Details -->
          <div class="flex-grow text-center sm:text-left">
            <h1
              class="text-3xl sm:text-4xl font-bold text-gray-900 mb-2 flex items-center justify-center sm:justify-start"
            >
              <%= pet.petName %>
              <span class="ml-2 text-2xl" role="img" aria-label="dog emoji"
                ><%= pet.emoji %></span
              >
            </h1>
            <p class="text-gray-700 text-lg mb-1">
              <span class="font-semibold">Breed:</span> <%= pet.breed %>
            </p>
            <p class="text-gray-700 text-lg mb-1">
              <span class="font-semibold">Age:</span> <%= pet.age %> Years
            </p>
            <p class="text-gray-700 text-lg mb-1">
              <span class="font-semibold">Gender:</span> <%= pet.gender %>
            </p>
            <p class="text-gray-700 text-lg mb-1">
              <span class="font-semibold">Species:</span> <%= pet.species %>
            </p>
            <p class="text-gray-700 text-lg mb-1">
              <span class="font-semibold">Color:</span> <%= pet.color %>
            </p>
            <p class="text-gray-700 text-lg mb-1">
              <span class="font-semibold">Date of birth:</span> <%=
              pet.dateOfBirth %>
            </p>
            <p class="text-gray-700 text-lg mb-1">
              <span class="font-semibold">Responsible Veterenian:</span> <%=
              pet.responsibleVet %>
            </p>
            <p class="text-gray-700 text-lg mb-4">
              <span class="font-semibold">Medical History:</span> <%=
              pet.medicalHistory %>
            </p>

            <!-- Action Buttons -->
            <div
              class="flex flex-col sm:flex-row justify-center sm:justify-start space-y-3 sm:space-y-0 sm:space-x-4"
            >
              <button
                class="bg-teal-500 hover:bg-teal-600 text-white font-bold py-3 px-6 rounded-lg shadow-md transition duration-300 ease-in-out transform hover:scale-105"
              >
                Book Appointment
              </button>
              <form action="/edit-profile/<%=pet._id%>" method="get">
                <button
                  class="bg-yellow-500 hover:bg-yellow-600 text-white font-bold py-3 px-6 rounded-lg shadow-md transition duration-300 ease-in-out transform hover:scale-105"
                  type="submit"
                >
                  Edit Details
                </button>
              </form>
            </div>
          </div>
        </div>
      </div>

      <!-- Owner Details Section -->
      <div class="max-w-4xl mx-auto bg-white shadow-lg rounded-xl p-6 sm:p-8">
        <h2 class="text-2xl sm:text-3xl font-bold text-gray-900 mb-4">
          Owner Details
        </h2>
        <p class="text-gray-700 text-lg mb-1">
          <span class="font-semibold">Name:</span> <%= pet.ownerName %>
        </p>
        <p class="text-gray-700 text-lg mb-1">
          <span class="font-semibold">Phone:</span> <%= pet.phone %>
        </p>
        <p class="text-gray-700 text-lg mb-1">
          <span class="font-semibold">Address:</span> <%= pet.address %>
        </p>
      </div>
    </div>
  </body>
</html>
